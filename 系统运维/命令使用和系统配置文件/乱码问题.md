# 乱码问题
```
乱码描述：开机自动执行的程序、crontab运行的程序出现乱码，手动执行没有问题

乱码原因：这是因为Unix/Linux下系统自动执行的程序和crontab时的运行环境已经不是用户环境了，因此原本用户下的一些环境变量的设置就失效了。例如原来用户环境变量设置的是GB2312，但是使用crontab运行时的环境变量就可能是ISO8859-1。

解决方法：
首先执行命令echo $LANG，获取输出，比如是en_US.UTF-8，然后在脚本中增加一行export LANG=en_US.UTF-8就可以了
```